apiVersion: apps/v1
kind: Deployment
metadata:
  name: kb-web
  namespace: kb-learning
spec:
  template:
    spec:
      containers:
        - name: web
          image: nginx:does-not-exist-kb-learning
